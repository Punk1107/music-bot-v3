services:
  - type: web
    name: music-bot-v3
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: "8080"
    healthCheckPath: /health
    autoDeploy: true
